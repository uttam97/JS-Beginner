const name = "Uttam"
const repoCount = 40

//console.log(name+repoCount+" Value")

//console.log(`my name is ${name} and my repo count is ${repoCount}`);

const str = new String("Uttam")

console.log(str);
//toUpperCase, length, charAt, indexOf, substring, slice(we can give negative number)
//trim,split, replace

